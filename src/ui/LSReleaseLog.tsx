import React from "react"
import "../styles/LSReleaseLog.css"

export const LSReleaseLog = (): React.JSX.Element => {
    return (
        <div className={`ls-release-log-wrapper`}>
            <h1>LS Release Log</h1>
        </div>
    )
}